class GroupdModel {
  constructor(groupId, groupName, groupAlbums) {
    // a string
    this.groupId = groupId;

    // a string
    this.groupName = groupName;

    // a list of albumModel
    this.groupAlbums = groupAlbums;
  }
}

export default GroupdModel;
